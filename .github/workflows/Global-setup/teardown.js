// global-teardown.js
async function globalTeardown(config) {
  console.log('Running global teardown...');

  // Example: cleanup (API call / DB reset / delete files)
}

export default  globalTeardown;